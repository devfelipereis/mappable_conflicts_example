name: mappable_conflicts_example
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dart_mappable: ^4.0.0
  base:
    path: packages/base
  ivy:
    path: packages/ivy
  hexa:
    path: packages/hexa

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  dart_mappable_builder: ^4.0.0

flutter:
  uses-material-design: true